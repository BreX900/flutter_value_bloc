name: example
description: Example
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_value_bloc:
    path: ../../flutter_value_bloc

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
